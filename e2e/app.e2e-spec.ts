import { MidasPage } from './app.po';

describe('decima App', () => {
  let page: MidasPage;

  beforeEach(() => {
    page = new MidasPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
